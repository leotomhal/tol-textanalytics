/**
 * Einmalig auszuführendes Skript: wandelt die DeReWo-Rohliste des IDS
 * Mannheim in `src/data/derewo-top20k.json` um (stemmen, auf Top 20.000
 * kürzen). Wird in M2 implementiert (siehe konzept-textanalyse-plugin.md,
 * Abschnitt 3.5 und Phase 1).
 */

export {};
